# FR-12: Dead-Reckoning Sensor Fusion And Drift Correction

## Requirement
> The system shall fuse dead reckoning estimates with periodic visual fixes to
> reset accumulated drift errors.

## Rationale
When GNSS is degraded, denied, or unavailable, the navigation stack still
needs a continuous position estimate between visual localizations. FR-10
introduced a standalone IMU-based dead-reckoning source, and FR-11 provides
periodic absolute visual pose fixes. This requirement connects the two: dead
reckoning acts as the continuous slave estimate, while each trusted visual fix
acts as the master correction that re-anchors the DR state and clears
accumulated drift. Healthy GPS fixes also remain trusted corrections, and DR
must only be used as a bounded short bridge whose confidence degrades with time
since the last trusted correction.

## Acceptance Criteria
1. **[Met]** Dead reckoning is used as the continuous estimate when GNSS is
   degraded, denied, or unavailable.
2. **[Met]** Each valid high-confidence visual fix and each healthy GPS fix
   resets accumulated dead-reckoning drift by correcting the DR state to the
   trusted pose and refreshing bridge age/confidence.
3. **[Met]** Dead-reckoning confidence decays with time since the last trusted
   correction, and the DR bridge expires after a configured maximum duration.
4. **[Met]** From the point of view of current `PositionBlender` consumers, the
   output format remains unchanged: a geodetic `(lat, lon, alt)` tuple plus a
   navigation-mode string.
5. **[Met]** The GNSS-healthy fast path remains preserved and uses GPS directly
   as the trusted source.
6. **[Met]** When no valid visual fix is available, the system continues in
   DR-only mode only while the bridge remains valid; otherwise it enters
   failsafe rather than treating stale DR as reliable.
7. **[Met]** Unit and integration tests validate visual/GPS correction, bridge
   expiry, output compatibility, DR-only behavior, and the preserved
   GPS-healthy path.

## Dependencies
FR-10, FR-9

## Linked Source Files
- `src/vns/core/blender.py` — FR-12 fusion logic, DR-only fallback, and visual-master reset handling.
- `src/vns/core/runtime.py` — packaged runtime wiring for IMU-fed dead reckoning and blender integration.
- `src/vns/core/vns_node.py` — ROS entrypoint that now forwards IMU motion data and publishes DR-backed estimates.
- `src/vns/core/dead_reckoning.py` — FR-10 dead-reckoning estimate source consumed by the FR-12 fusion path.
- `src/vns/config/models.py` — typed config surface for DR bias values, bridge duration, and confidence decay.
- `simulation/config/simulation.yaml` — default conservative DR bridge settings for the packaged runtime.
- `tests/test_blender.py` — direct FR-12 drift-reset, DR-only, and output-compatibility coverage.
- `tests/test_runtime.py` — packaged-runtime integration coverage for DR fallback and visual reset behavior.

## Test Plan

### Unit Tests
- Verify a high-confidence visual fix snaps the fused pose to the visual
  correction and resets the dead-reckoning state.
- Verify a healthy GPS correction re-anchors the DR bridge after expiry.
- Verify subsequent DR propagation continues from the corrected pose instead of
  the pre-reset drifted pose.
- Verify DR-only operation continues without visual fixes only while the
  bridge remains valid and transitions to failsafe after expiry.
- Verify the GPS-healthy path preserves the existing output contract and does
  not allow DR/vision corrections to override healthy GPS output.

### Integration Tests
- Verify `VnsRuntime` supplies IMU-driven dead-reckoning estimates to the
  blender when localization fails under GNSS denial.
- Verify `VnsRuntime` passes successful visual fixes through the blender and
  resets the underlying dead-reckoning state.
- Re-run the core/runtime/interface regression surface to confirm logger,
  MAVLink-facing, GNSS-monitor, and legacy blender behavior remain compatible.

### Simulation Tests
- Future follow-up: validate live PX4 SITL / Gazebo behavior with actual IMU,
  GNSS denial, and periodic visual fixes in-flight. This repository change does
  not include a full SITL run.

## Status
- [ ] Not Started
- [ ] In Progress
- [x] Done

**Implementation status:** Implemented.

**Implementation evidence:** Fusion logic now lives in `src/vns/core/blender.py`
and consumes the FR-10 dead-reckoning estimate from
`src/vns/core/dead_reckoning.py`. Dead reckoning is treated as the continuous
slave estimate when GNSS is not healthy, healthy GPS and accepted visual fixes
both re-anchor the DR state, DR confidence decays with correction age, the
bridge is capped by a conservative runtime limit, the GPS-healthy fast path is
preserved, and the geodetic output consumed by `src/vns/core/vns_node.py`
remains unchanged. `src/vns/core/runtime.py::get_diagnostics()` now reports a
`dead_reckoning` subsystem exposing FR-10's timestamp-anomaly counters
(duplicate/stale/reset/clamped) and last accepted timestamp, with throttled
warning logs on each anomaly; `vns_node.py::_on_imu` is now exception-guarded
so no IMU callback failure can terminate `rclpy.spin()`.

**Test evidence (2026-06-30):**
- `python3 -m pytest tests/test_blender.py -q` passed (`4 passed`).
- `python3 -m pytest tests/test_runtime.py -q` passed (`16 passed`).
- `python3 -m pytest tests/unit/test_dead_reckoning.py tests/test_blender.py tests/test_runtime.py tests/test_simulation_integration.py -q` passed (`36 passed`).
- `python3 -m pytest tests/ -q` passed (`229 passed, 7 skipped`).

**Test evidence (2026-07-01, IMU timestamp-robustness fix):**
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_runtime.py -q` passed (`20 passed`) — adds `update_imu` duplicate/stale/reset-without-raising and `dead_reckoning` diagnostics-subsystem coverage.
- `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 python3 -m pytest tests/test_vns_node.py -q` passed (`4 passed`) — new file; covers `_on_imu` field forwarding and exception-swallowing.
